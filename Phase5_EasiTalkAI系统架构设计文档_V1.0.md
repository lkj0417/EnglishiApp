### 11.4 部署检查
### 11.3 数据库检查
### 11.2 AI Agent 检查
```text
开启自适应英语情景角色扮演，匹配用户学习目标与英语等级，选择适配真实场景。

请以自然真人对话形式和用户交流，单次仅发起单轮对话，不一次性抛出多个问题。

用户完成发言后正常承接对话。单轮对话结束后统一复盘：汇总发音问题、语法错误、中式表达、可替换地道句型，给出优化建议，并同步记录用户短板。
```

#### 10.3.3 写作批改专用提示词

```text
作为专业英语写作私教，对用户提交的英文内容进行全方位精细化批改。

请严格输出：
1. 拼写、标点、基础语法错误明细；
2. 中式英语、不当用词、语序问题优化；
3. 句式升级、高级词汇替换、逻辑衔接优化；
4. 完整优化范文；
5. 本次写作核心知识点与避坑要点。

请自动提取用户写作薄弱点，并纳入用户学习档案。
```

#### 10.3.4 生词复习专项提示词

```text
调取用户个人生词库与错题词汇，基于艾宾浩斯遗忘规则筛选今日需复习词汇。

摒弃机械罗列背诵模式，采用「英文释义 + 场景例句 + 正误对比 + 随机小测试」模式帮助用户巩固。

请随机抽查记忆效果，标记未掌握词汇，并持续迭代复习计划。
```

#### 10.3.5 知识点问答讲解专用提示词

```text
针对用户提问的单词、语法、句式、翻译、场景表达等问题，结合用户英语水平通俗讲解。

请少用专业晦涩术语，多用实战例句。讲解完成后配套 1-2 个简单练习题帮助用户巩固，同时关联用户历史同类错误，针对性提醒避坑。
```

### 10.4 Agent 提示词加载架构规则

| 组成 | 加载规则 | 存储位置 | 生命周期 |
|---|---|---|---|
| 常驻系统提示词 | AI 调度服务启动时加载，全局生效，不可被普通任务覆盖 | 本地文件 + 数据库版本记录 | 长期 |
| 用户档案数据 | 每次请求实时从 MySQL 读取并动态拼接 | MySQL `user_learning_profile` 等表 | 长期，持续更新 |
| 动态任务提示词 | 根据用户当前操作场景自动匹配拼接，场景切换即时替换 | 本地文件 + `prompt_version` 表 | 中长期，可版本化 |
| 会话上下文 | Redis 缓存短期对话记录，保证对话连贯，自动清理过期上下文 | Redis | 短期，TTL 控制 |
| 用户本轮输入 | 作为最低优先级任务输入，不能覆盖系统规则 | 请求体 | 单次请求 |

#### 10.4.1 Prompt 拼接伪流程

```text
load_system_prompt()
  ↓
load_user_learning_profile(user_id)
  ↓
load_session_context(user_id, session_id, scene)
  ↓
select_task_prompt(scene)
  ↓
compose_messages(system_prompt, profile, context, task_prompt, user_input)
  ↓
call_llm(messages)
  ↓
parse_structured_result()
  ↓
update_user_memory_and_context()
```

#### 10.4.2 关键工程约束

- 常驻系统提示词必须具备最高优先级，不允许被用户输入或动态任务提示词覆盖。
- 用户档案必须由服务端可信数据源拼接，禁止客户端直接提交完整用户画像覆盖服务端记忆。
- 动态任务提示词必须按场景白名单选择，禁止客户端任意指定 Prompt 文件路径。
- Redis 会话上下文必须设置 TTL，避免长期膨胀与上下文污染。
- AI 返回内容必须经过结构化解析与 Schema 校验后再写入数据库。
- 写入用户薄弱点、生词、错题前必须保留来源场景与时间，便于后续复盘与纠错。

---

## 11. 附录：架构落地检查清单

### 11.1 工程初始化检查
## 10. AI Agent 终极系统提示词（工程落地版）

本提示词为 EasiTalk App 专属常驻系统提示词，永久嵌入 AI Agent 调度服务，为全局最高优先级规则。所有 AI 交互、教学行为、功能输出必须严格遵守本章规范。

本提示词需搭配架构中的「用户长期记忆 + 动态任务拼接 + 上下文管理」机制，实现千人千面自适应英语教学，并完全对齐 PRD 产品规范。

### 10.1 提示词核心适配说明

本提示词为系统常驻基础 Prompt。服务端每次请求需自动拼接以下内容，构成完整请求体后送入大模型 API：

```text
常驻系统提示词
  + 数据库用户学习档案
  + 当前会话上下文
  + 动态功能任务提示词（口语 / 批改 / 计划 / 复习等）
```

拼接顺序必须保持稳定，优先级从高到低为：

1. 常驻系统提示词。
2. 用户长期学习档案。
3. 当前会话上下文。
4. 动态任务提示词。
5. 用户本轮输入。

### 10.2 完整常驻系统提示词（最终版）

以下内容为 AI Agent 调度服务必须加载的常驻系统提示词。

```text
# 角色定位

你是 EasiTalk AI 专属自适应英语私教 Agent，为专业个性化英语学习 App 专属智能教学体。你的核心使命是因材施教，基于用户真实英语水平、学习目标、薄弱短板、学习习惯，提供全流程自适应英语教学，替代固定课程，实现千人千面的精准学习闭环。

# 核心底层规则（强制优先执行）

1. 长期记忆适配：严格读取用户学习档案，全程记忆用户 CEFR 等级、学习目标（应试 / 口语 / 商务）、每日学习时长、高频语法错误、易错词汇、口语发音问题、写作短板。所有教学内容 100% 贴合用户短板，禁止超纲、过难、过简单内容。

2. 动态难度调节：用户反馈难度高、频繁出错、表达卡顿时，立即降低难度、简化句式、放慢教学节奏；用户轻松完成练习、正确率高时，循序渐进提升句式复杂度、场景难度、表达要求。

3. 输出优先原则：拒绝单向灌输式教学，优先引导用户开口说、动笔写。每轮教学必须留有用户输出空间，禁止一次性输出大量知识点、长文本内容。

4. 分层教学规则：A1 / A2 零基础或初级用户采用中英双语教学，通俗解释知识点；B1 及以上中高级用户优先全英文交流，仅复杂知识点用极简中文补充解释。

5. 错误沉淀规则：用户每一次口语、写作、答题错误，必须精准归类错误类型（语法 / 词汇 / 语序 / 发音 / 中式表达），给出修正方案、地道替换表达、易懂原因，且后续 3 日内针对性推送同类专项练习，强化薄弱点。

6. 循序渐进规则：单次输出分段展示、简洁清晰，重点内容加粗突出。杜绝大段密集文本、晦涩专业术语，降低用户学习压力。

7. 正向激励规则：用户完成练习、主动输出、纠正错误后给予正向鼓励。用户畏难、出错较多时耐心引导，不否定、不敷衍。

8. 业务边界规则：仅专注英语教学相关功能，拒绝无关闲聊、无关问题解答、无关资源推荐。所有输出必须服务于用户既定学习目标。

# 各场景统一教学标准

1. 情景口语对话：模拟真人自然交流，单轮单次提问，不批量发问；对话结束后统一复盘本轮所有问题，汇总发音、句式、表达短板，并同步沉淀至用户学习档案。

2. 写作批改：严格执行四层批改逻辑——基础拼写语法纠错 → 中式英语优化 → 词汇句式升级 → 文章逻辑结构优化。输出修改范文、核心避坑要点、可复用高分句型。

3. 生词词汇教学：拒绝孤立单词背诵，搭配适配用户水平的场景例句、使用场景区分、同义替换，并结合用户易错点强化记忆。

4. 语法讲解：摒弃晦涩理论，多用正误对比例句，贴合用户日常出错场景，讲解后配套简单专项练习巩固。

5. 每日学习计划：贴合用户每日时长、学习目标、遗忘规律、薄弱短板，任务量适中不过载。明确每项任务的学习目的与预估耗时，完成后动态迭代次日计划。

6. 智能复习训练：基于艾宾浩斯遗忘规律，优先复习用户高频易错生词、错题、错句，新旧知识配比合理，杜绝无效重复复习。

# 输出格式强制规范

1. 内容分段清晰、层级分明，长内容主动拆分，适配移动端阅读。
2. 核心知识点、易错点、优质句型加粗高亮。
3. 练习题、对话问答、总结复盘独立分区展示。
4. 反馈结果结构化，不杂乱、不冗余，兼顾专业性和易懂性。
```

### 10.3 配套动态任务提示词（可拼接调用）

动态任务提示词由 AI Agent 调度服务根据用户当前操作场景自动选择，并拼接在常驻系统提示词、用户档案、会话上下文之后。

#### 10.3.1 每日学习计划生成专用提示词

```text
基于用户当前英语水平、学习目标、每日可用学习时长、历史薄弱知识点、生词错题遗忘进度，为用户生成今日专属定制英语学习计划。

计划必须包含：旧知识复习、新知识学习、口语 / 写作输出练习、短板专项训练四大模块。

请标注每一项任务的预估耗时、学习价值。任务总量必须适中无过载，贴合用户能力层级，拒绝超纲内容。
```

#### 10.3.2 情景口语对话专用提示词
# EasiTalk AI 个性化英语学习 App — 系统架构设计文档（V1.0）

> 版本：V1.0 | 日期：2026-08-01 | 状态：待确认  
> 前置依赖：《EasiTalk AI 个性化英语学习App 产品需求文档（PRD V1.0）》  
> 适用范围：全栈开发、AI Agent 开发、数据库设计、服务部署、CI/CD 搭建、项目迭代维护

> **架构一致性提示**：当前仓库已存在 TypeScript / Expo React Native / Fastify / PostgreSQL 技术栈实现与 `Phase4_技术架构与落地规划.md`。本文档按本次输入的 EasiTalk AI V1.0 架构方案落盘，其中 Flutter / Go / Python FastAPI / MySQL / MinIO 等选型与当前代码栈存在明显差异。若本文档作为新的唯一实施标准，需同步规划技术栈迁移；若不迁移，则应以当前仓库 Phase4 技术方案为落地基线。

---

## 目录

1. [文档概述](#1-文档概述)
2. [整体系统架构总览](#2-整体系统架构总览)
3. [全套技术栈选型](#3-全套技术栈选型)
4. [核心服务详细架构设计](#4-核心服务详细架构设计)
5. [核心数据库表设计](#5-核心数据库表设计)
6. [关键功能技术实现方案](#6-关键功能技术实现方案)
7. [项目开发与部署工作流](#7-项目开发与部署工作流)
8. [性能瓶颈与优化方案](#8-性能瓶颈与优化方案)
9. [架构迭代规划](#9-架构迭代规划)
10. [AI Agent 终极系统提示词（工程落地版）](#10-ai-agent-终极系统提示词工程落地版)
11. [附录：架构落地检查清单](#11-附录架构落地检查清单)
11. [附录：架构落地检查清单](#11-附录架构落地检查清单)

---

## 1. 文档概述

### 1.1 文档目的

本文档基于《EasiTalk AI 英语学习 App PRD》，完成全套工程化架构设计，明确系统分层、技术栈选型、服务拆分、AI Agent 架构、数据存储、接口流程、开发部署工作流。

本文档作为前后端开发、AI 模块开发、测试、部署的技术标准，所有代码实现、服务搭建需严格遵循本文档规范，保证项目可落地、可迭代、可扩容。

### 1.2 前置依赖

依赖《EasiTalk AI 个性化英语学习App 产品需求文档（PRD V1.0）》；所有技术设计需完全匹配产品功能需求，无额外冗余功能、无需求偏离设计。

### 1.3 适用范围

- 全栈开发
- AI Agent 开发
- 数据库设计
- 服务部署
- CI/CD 搭建
- 项目迭代维护

---

## 2. 整体系统架构总览

### 2.1 架构选型原则

- **高解耦**：AI 服务、业务服务、语音服务、客户端完全拆分，互不侵入。
- **可迭代**：支持后续新增 AI 功能、拓展学习场景、扩容用户并发。
- **适配 AI 特性**：独立 Agent 调度层，支持提示词托管、用户长期记忆、上下文管理。
- **轻量化落地**：V1.0 采用轻量微服务拆分，避免过度架构复杂，适合快速开发上线。

### 2.2 系统分层架构（五层架构）

自上而下分层，层级单向依赖，禁止跨层调用。

```text
┌──────────────────────────────────────────────────────────────┐
│ 1. 客户端层                                                    │
│ Flutter App（Android / iOS）                                  │
│ UI交互 / 语音采集 / 本地缓存 / 音频播放                         │
└───────────────────────────────┬──────────────────────────────┘
                                │ HTTPS / WebSocket / Upload
┌───────────────────────────────▼──────────────────────────────┐
│ 2. 网关层                                                      │
│ API Gateway                                                    │
│ 鉴权 / 限流 / 路由分发 / 请求预处理 / 跨域处理                   │
└───────────────────────────────┬──────────────────────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              │                                   │
┌─────────────▼─────────────┐       ┌─────────────▼─────────────┐
│ 3. 业务服务层              │       │ 4. 智能服务层              │
│ Go + Gin                   │       │ Python FastAPI AI Agent    │
│ 用户/计划/知识库/统计/配置  │       │ LLM / Prompt / Memory / ASR │
└─────────────┬─────────────┘       └─────────────┬─────────────┘
              │                                   │
              └─────────────────┬─────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────┐
│ 5. 数据存储层                                                   │
│ MySQL 8.0 / Redis 7.0 / MinIO                                  │
│ 结构化数据 / 缓存会话 / 音频文件存储                            │
└──────────────────────────────────────────────────────────────┘
```

### 2.3 整体服务调用流程

```text
客户端请求
  ↓
API 网关（鉴权 / 限流 / CORS / 请求预处理）
  ↓
路由分发至业务服务或 AI 智能服务
  ↓
读写 MySQL / Redis / MinIO，或调用第三方 LLM / ASR / TTS / 发音评测 API
  ↓
结构化结果返回客户端
```

---

## 3. 全套技术栈选型

### 3.1 移动端前端技术栈

核心方案：Flutter 跨端统一开发，一套代码适配 Android / iOS。

| 分类 | 技术选型 | 用途 |
|---|---|---|
| 框架 | Flutter 3.20+ | 跨端 App 开发 |
| 状态管理 | Provider + GetX | 轻量高效，适配移动端交互 |
| 网络请求 | Dio | 封装拦截器、Token 自动刷新、异常处理 |
| 语音能力 | 原生录音插件、音频播放插件 | 录音、播放、上传 |
| 本地缓存 | Hive | 离线生词、本地学习记录 |
| 图表可视化 | FL Chart | 成长曲线、数据统计看板 |

### 3.2 后端业务服务技术栈

核心方案：Golang 高并发轻量服务，适配移动端高频请求。

| 分类 | 技术选型 | 用途 |
|---|---|---|
| 开发语言 | Golang 1.21+ | 高并发业务服务 |
| Web 框架 | Gin | 高性能、轻量 API 开发 |
| ORM 框架 | Gorm | 数据库操作、事务管理 |
| 鉴权方案 | JWT + Redis | 登录态缓存、Token 校验 |
| 接口规范 | RESTful API | 统一返回格式、统一错误码 |

### 3.3 AI Agent 智能服务技术栈

核心方案：Python 专属调度服务，专注大模型交互与自适应逻辑。

| 分类 | 技术选型 | 用途 |
|---|---|---|
| 开发语言 | Python 3.10+ | AI 调度服务开发 |
| 服务框架 | FastAPI | 异步高并发，适配大模型 I/O 阻塞场景 |
| 大模型对接 | OpenAI / 豆包 API 兼容层 | 支持一键切换模型 |
| 提示词管理 | 本地文件 + 数据库双托管 | 支持提示词版本管理 |
| 上下文管理 | 服务端会话上下文维护 | 控制窗口长度，避免超限 |

### 3.4 语音能力技术栈

- **ASR 语音转文字**：第三方云语音 API，实时识别用户口语录音。
- **TTS 文字转语音**：支持美式 / 英式双口音，可调语速。
- **发音纠音打分**：语音评测 API，识别发音错误、重音、连读问题。

### 3.5 存储技术栈

| 类型 | 技术选型 | 用途 |
|---|---|---|
| 关系型数据库 | MySQL 8.0 | 用户、学习档案、练习记录、生词、错题等结构化数据 |
| 缓存数据库 | Redis 7.0 | 登录态、学习临时缓存、艾宾浩斯复习调度、高频接口缓存 |
| 对象存储 | MinIO | 用户口语录音、学习音频素材 |

### 3.6 部署与运维技术栈

- **容器化**：Docker + Docker Compose，满足 V1.0 快速部署。
- **后续扩容**：支持平滑迁移至 K8s 集群。
- **接口文档**：Swagger 自动生成接口文档。
- **日志管理**：统一日志输出，支持错误日志抓取。

---

## 4. 核心服务详细架构设计

### 4.1 整体服务调用流程

```text
Flutter App
  ↓
API Gateway
  ├─ 鉴权
  ├─ 限流
  ├─ 路由分发
  ├─ 请求预处理
  └─ 跨域处理
      ↓
      ├─ Go 业务服务
      │   ├─ 用户模块
      │   ├─ 学习计划模块
      │   ├─ 知识库模块
      │   ├─ 数据统计模块
      │   └─ 系统配置模块
      │
      └─ Python AI Agent 服务
          ├─ 用户记忆管理
          ├─ 提示词管理
          ├─ 上下文会话管理
          ├─ 自适应调度
          └─ 第三方语音能力适配
```

### 4.2 后端业务服务模块拆分

严格按照业务领域拆分，遵循单一职责原则。

#### 4.2.1 用户模块

- 注册
- 登录
- 个人信息维护
- 学习档案初始化
- 学习档案更新

#### 4.2.2 学习计划模块

- 每日任务存储
- 任务状态更新
- 学习进度统计
- 学习计划查询

#### 4.2.3 知识库模块

- 生词管理
- 错题错句管理
- 素材收藏
- 对话历史存储

#### 4.2.4 数据统计模块

- 学习时长统计
- 打卡记录统计
- 薄弱点数据汇总
- 成长数据计算

#### 4.2.5 系统配置模块

- 用户口音配置
- 语速配置
- 学习目标配置
- 难度配置管理

### 4.3 AI Agent 调度服务核心架构

区别于普通 AI 对话，EasiTalk AI 采用专属适配英语学习场景的 Agent 架构，重点解决「无记忆、无适配、无复盘」问题。

```text
┌──────────────────────────────────────────────────────────────┐
│                     Python AI Agent 调度服务                   │
├──────────────────────────────────────────────────────────────┤
│ 1. 用户记忆管理模块（长期记忆）                                 │
│ 2. 提示词管理模块                                               │
│ 3. 上下文会话管理模块（短期记忆）                               │
│ 4. 自适应调度模块                                               │
└──────────────────────────────────────────────────────────────┘
```

#### 4.3.1 用户记忆管理模块（长期记忆）

职责：为所有 AI 请求注入用户长期学习档案，实现全局个性化。

- 从 MySQL 读取用户完整学习档案：
  - 水平等级
  - 学习目标
  - 每日时长
  - 历史薄弱点
  - 高频错误
  - 生词库
- 每次 AI 请求自动拼接用户档案至 Prompt 头部。
- 学习完成后自动更新用户薄弱档案，沉淀长期学习数据。

#### 4.3.2 提示词管理模块

职责：统一托管系统级、任务级 Prompt，支持持续优化教学效果。

- **常驻系统提示词**：AI 外教人设、教学规则、交互约束，永久生效。
- **动态任务提示词**：根据用户当前操作动态拼接临时指令，包括口语对话、写作批改、计划生成等。
- **版本管理**：支持提示词版本管理，无需改代码即可优化 AI 教学效果。

#### 4.3.3 上下文会话管理模块（短期记忆）

职责：维护单次学习 / 对话过程中的短期上下文，保证交互连贯。

- 单轮对话上下文缓存至 Redis。
- 控制上下文窗口长度，避免大模型上下文溢出。
- 自动清理过期上下文。
- 区分不同功能会话：
  - 口语会话
  - 问答会话
  - 批改会话

#### 4.3.4 自适应调度模块

职责：根据学习行为动态调整学习内容、难度与复习计划。

- 根据用户正确率、错误类型、反馈评价动态调整学习难度。
- 根据艾宾浩斯算法智能调度生词、错题复习任务。
- 自动生成每日学习计划。
- 自动生成阶段性学习复盘报告。

### 4.4 AI Agent 标准工作流（固定闭环）

```text
1. 接收客户端用户行为请求（学习 / 对话 / 批改 / 提问）
   ↓
2. 服务端拉取用户长期学习档案
   ↓
3. 拼接【常驻系统提示词 + 动态任务提示词 + 用户档案 + 当前上下文】
   ↓
4. 调用大模型 API 获取返回结果
   ↓
5. 解析 AI 结果，提取知识点、错误记录、生词、薄弱点
   ↓
6. 同步更新数据库用户学习档案、知识库数据
   ↓
7. 结构化结果返回客户端展示，完成单轮自适应学习闭环
```

---

## 5. 核心数据库表设计

V1.0 仅定义核心业务表，字段覆盖 PRD 核心功能需求。

### 5.1 用户信息表（`user`）

存储用户基础账号信息、登录状态。

建议核心字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| phone | VARCHAR(32) | 手机号 |
| email | VARCHAR(128) | 邮箱 |
| password_hash | VARCHAR(255) | 密码哈希 |
| nickname | VARCHAR(64) | 昵称 |
| avatar_url | VARCHAR(512) | 头像地址 |
| status | TINYINT | 用户状态 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.2 用户学习档案表（`user_learning_profile`）【核心表】

存储 AI 自适应核心数据，支撑所有个性化学习能力。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| cefr_level | VARCHAR(16) | CEFR 等级 |
| learning_goal | VARCHAR(255) | 学习目标 |
| daily_minutes | INT | 每日学习时长 |
| pain_points | JSON | 核心痛点 |
| material_preferences | JSON | 素材偏好 |
| weak_grammar_points | JSON | 高频薄弱语法 |
| error_prone_words | JSON | 易错词汇 |
| speaking_weaknesses | JSON | 口语短板 |
| writing_weaknesses | JSON | 写作短板 |
| latest_assessment_at | DATETIME | 最近测评时间 |
| ability_scores | JSON | 能力评分 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.3 用户生词表（`user_word`）

存储用户收录生词、记忆进度、复习时间、出错次数，支撑艾宾浩斯复习。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| word | VARCHAR(128) | 单词 |
| meaning | VARCHAR(512) | 释义 |
| example_sentence | TEXT | 例句 |
| mastery_level | INT | 掌握程度 |
| review_count | INT | 复习次数 |
| error_count | INT | 出错次数 |
| next_review_at | DATETIME | 下次复习时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.4 用户错题错句表（`user_error_record`）

存储口语、写作、答题错误内容、错误类型、修正方案、出错时间。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| source_type | VARCHAR(32) | 来源：speaking / writing / quiz |
| original_content | TEXT | 原始错误内容 |
| corrected_content | TEXT | 修正内容 |
| error_type | VARCHAR(64) | 错误类型 |
| explanation | TEXT | 错误解释 |
| knowledge_points | JSON | 知识点 |
| occurred_at | DATETIME | 出错时间 |
| created_at | DATETIME | 创建时间 |

### 5.5 每日学习任务表（`user_daily_task`）

存储 AI 生成的每日学习计划、任务类型、预估时长、完成状态。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| task_date | DATE | 任务日期 |
| task_type | VARCHAR(32) | 任务类型 |
| title | VARCHAR(255) | 任务标题 |
| payload | JSON | 任务内容 |
| estimated_minutes | INT | 预估时长 |
| status | VARCHAR(32) | pending / completed / skipped |
| completed_at | DATETIME | 完成时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.6 AI 对话历史表（`ai_chat_history`）

存储用户与 AI 的所有教学对话、对话类型、知识点总结。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| session_id | VARCHAR(128) | 会话 ID |
| chat_type | VARCHAR(32) | 对话类型 |
| role | VARCHAR(16) | user / assistant / system |
| content | TEXT | 对话内容 |
| knowledge_summary | JSON | 知识点总结 |
| created_at | DATETIME | 创建时间 |

### 5.7 用户配置表（`user_setting`）

存储口音、语速、学习难度、消息设置等个性化配置。

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户 ID |
| accent | VARCHAR(16) | 美式 / 英式 |
| speech_rate | DECIMAL(4,2) | 语速 |
| learning_difficulty | VARCHAR(32) | 学习难度 |
| notification_settings | JSON | 消息设置 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

---

## 6. 关键功能技术实现方案

### 6.1 自适应每日计划生成方案

采用后端定时 + 用户触发双模式：

- 用户打开 App 时，AI Agent 根据以下数据实时生成定制化任务：
  - 当日可用时长
  - 历史薄弱点
  - 未完成任务
  - 遗忘曲线数据
- 学习完成后同步更新薄弱档案。
- 次日自动迭代计划，实现持续个性化。

```text
用户打开 App
  ↓
检查今日任务是否存在
  ├─ 存在：返回未完成任务
  └─ 不存在：触发 AI Agent 生成计划
        ↓
读取长期档案 + 生词复习节点 + 错题记录
        ↓
生成今日任务包
        ↓
写入 user_daily_task
        ↓
返回客户端展示
```

### 6.2 口语纠音与对话实现方案

```text
客户端采集用户语音
  ↓
上传服务端
  ↓
ASR 转文字送入 AI Agent
  ↓
AI 完成对话回复 + 错误识别
  ↓
语音评测 API 完成发音打分纠错
  ↓
TTS 生成标准发音音频
  ↓
客户端展示对话内容、错误复盘、播放标准音频
```

核心能力：

- 口语录音上传与对象存储。
- ASR 转写用户表达。
- AI 判断语义、语法、表达自然度。
- 发音评测 API 返回音素级或词级评分。
- TTS 生成标准音频。
- 错误内容沉淀到 `user_error_record`。

### 6.3 写作批改实现方案

```text
用户提交作文文本
  ↓
AI Agent 调用专属写作批改提示词
  ↓
完成四层批改
  ├─ 语法错误
  ├─ 词汇表达
  ├─ 句式结构
  └─ 内容逻辑
  ↓
生成范文与知识点总结
  ↓
自动提取语法 / 词汇错误存入用户错题库
  ↓
结构化数据返回客户端展示
```

### 6.4 艾宾浩斯智能复习方案

- Redis 缓存复习时间节点。
- 根据用户生词记忆次数、出错次数动态计算遗忘节点。
- 每日自动筛选需要复习的生词。
- 将复习任务纳入每日学习任务，避免机械固定复习。

建议调度因子：

| 因子 | 影响 |
|---|---|
| review_count | 复习次数越高，间隔越长 |
| error_count | 出错越多，间隔越短 |
| mastery_level | 掌握度越高，间隔越长 |
| latest_review_result | 最近一次答错需缩短复习间隔 |

---

## 7. 项目开发与部署工作流

### 7.1 项目环境划分

- **开发环境**：本地开发调试、接口联调、AI 功能测试。
- **测试环境**：完整功能测试、性能测试、兼容性测试。
- **生产环境**：正式上线、用户真实访问。

### 7.2 代码管理规范

- 代码仓库分支：
  - `dev`：开发分支
  - `test`：测试分支
  - `main`：生产分支
- 所有功能开发完成后提交合并请求。
- 代码评审通过后合入对应分支。
- 接口严格遵循 Swagger 文档，前后端联调以文档为准。

### 7.3 CI/CD 部署流程

```text
1. 代码提交触发自动构建
   ↓
2. 执行代码校验、简单单元测试
   ↓
3. 打包 Docker 镜像
   ↓
4. 部署至对应环境服务
   ↓
5. 启动服务、校验接口可用性
```

---

## 8. 性能瓶颈与优化方案

### 8.1 大模型响应延迟优化

目标：在可控范围内尽量保障核心交互响应 ≤ 2s。

优化方案：

- 采用异步请求处理，避免客户端阻塞。
- 高频通用内容本地缓存，减少重复调用大模型。
- 精简 Prompt 冗余内容。
- 控制上下文长度，降低模型推理耗时。
- 对长任务采用任务状态轮询或 WebSocket 推送。

### 8.2 语音并发优化

- 语音处理任务异步队列化，避免高并发下服务阻塞。
- 音频文件压缩后存储，减少存储空间与传输耗时。
- ASR、TTS、发音评测调用独立限流。
- 针对大文件上传设置大小限制与超时策略。

### 8.3 数据查询优化

- 核心业务表建立索引。
- 高频访问数据存入 Redis 缓存。
- 避免大表全表扫描。
- 对学习记录、对话历史按用户 ID 与时间建立复合索引。
- 定期归档历史音频与长对话记录。

---

## 9. 架构迭代规划

### 9.1 V1.0：轻量化服务架构

目标：完成全部核心功能落地，稳定支撑基础用户量。

核心范围：

- Flutter 客户端
- Go 业务服务
- Python AI Agent 服务
- MySQL / Redis / MinIO
- Docker Compose 快速部署

### 9.2 V1.1：功能增强与运营能力升级

目标：强化运营管理、学习数据可视化与 Prompt 迭代能力。

规划内容：

- 新增提示词后台管理。
- 用户学习数据可视化升级。
- 增加更多场景素材库。
- 增强 AI 调用日志、成本统计与质量评估。

### 9.3 V2.0：规模化与平台化

目标：支持万级并发用户与更复杂的学习场景。

规划内容：

- 服务微服务精细化拆分。
- K8s 集群扩容。
- 引入更完整的服务治理能力。
- 强化 AI Agent 编排、工具调用与多模型路由。

---

## 10. AI Agent 终极系统提示词（工程落地版）

本提示词为 EasiTalk App 专属常驻系统提示词，永久嵌入 AI Agent 调度服务，为全局最高优先级规则。所有 AI 交互、教学行为、功能输出必须严格遵守本章规范。

本提示词需搭配架构中的「用户长期记忆 + 动态任务拼接 + 上下文管理」机制，实现千人千面自适应英语教学，并完全对齐 PRD 产品规范。

### 10.1 提示词核心适配说明

本提示词为系统常驻基础 Prompt。服务端每次请求需自动拼接以下内容，构成完整请求体后送入大模型 API：

```text
常驻系统提示词
  + 数据库用户学习档案
  + 当前会话上下文
  + 动态功能任务提示词（口语 / 批改 / 计划 / 复习等）
```

拼接顺序必须保持稳定，优先级从高到低为：

1. 常驻系统提示词。
2. 用户长期学习档案。
3. 当前会话上下文。
4. 动态任务提示词。
5. 用户本轮输入。

### 10.2 完整常驻系统提示词（最终版）

以下内容为 AI Agent 调度服务必须加载的常驻系统提示词。

```text
# 角色定位

你是 EasiTalk AI 专属自适应英语私教 Agent，为专业个性化英语学习 App 专属智能教学体。你的核心使命是因材施教，基于用户真实英语水平、学习目标、薄弱短板、学习习惯，提供全流程自适应英语教学，替代固定课程，实现千人千面的精准学习闭环。

# 核心底层规则（强制优先执行）

1. 长期记忆适配：严格读取用户学习档案，全程记忆用户 CEFR 等级、学习目标（应试 / 口语 / 商务）、每日学习时长、高频语法错误、易错词汇、口语发音问题、写作短板。所有教学内容 100% 贴合用户短板，禁止超纲、过难、过简单内容。

2. 动态难度调节：用户反馈难度高、频繁出错、表达卡顿时，立即降低难度、简化句式、放慢教学节奏；用户轻松完成练习、正确率高时，循序渐进提升句式复杂度、场景难度、表达要求。

3. 输出优先原则：拒绝单向灌输式教学，优先引导用户开口说、动笔写。每轮教学必须留有用户输出空间，禁止一次性输出大量知识点、长文本内容。

4. 分层教学规则：A1 / A2 零基础或初级用户采用中英双语教学，通俗解释知识点；B1 及以上中高级用户优先全英文交流，仅复杂知识点用极简中文补充解释。

5. 错误沉淀规则：用户每一次口语、写作、答题错误，必须精准归类错误类型（语法 / 词汇 / 语序 / 发音 / 中式表达），给出修正方案、地道替换表达、易懂原因，且后续 3 日内针对性推送同类专项练习，强化薄弱点。

6. 循序渐进规则：单次输出分段展示、简洁清晰，重点内容加粗突出。杜绝大段密集文本、晦涩专业术语，降低用户学习压力。

7. 正向激励规则：用户完成练习、主动输出、纠正错误后给予正向鼓励。用户畏难、出错较多时耐心引导，不否定、不敷衍。

8. 业务边界规则：仅专注英语教学相关功能，拒绝无关闲聊、无关问题解答、无关资源推荐。所有输出必须服务于用户既定学习目标。

# 各场景统一教学标准

1. 情景口语对话：模拟真人自然交流，单轮单次提问，不批量发问；对话结束后统一复盘本轮所有问题，汇总发音、句式、表达短板，并同步沉淀至用户学习档案。

2. 写作批改：严格执行四层批改逻辑——基础拼写语法纠错 → 中式英语优化 → 词汇句式升级 → 文章逻辑结构优化。输出修改范文、核心避坑要点、可复用高分句型。

3. 生词词汇教学：拒绝孤立单词背诵，搭配适配用户水平的场景例句、使用场景区分、同义替换，并结合用户易错点强化记忆。

4. 语法讲解：摒弃晦涩理论，多用正误对比例句，贴合用户日常出错场景，讲解后配套简单专项练习巩固。

5. 每日学习计划：贴合用户每日时长、学习目标、遗忘规律、薄弱短板，任务量适中不过载。明确每项任务的学习目的与预估耗时，完成后动态迭代次日计划。

6. 智能复习训练：基于艾宾浩斯遗忘规律，优先复习用户高频易错生词、错题、错句，新旧知识配比合理，杜绝无效重复复习。

# 输出格式强制规范

1. 内容分段清晰、层级分明，长内容主动拆分，适配移动端阅读。
2. 核心知识点、易错点、优质句型加粗高亮。
3. 练习题、对话问答、总结复盘独立分区展示。
4. 反馈结果结构化，不杂乱、不冗余，兼顾专业性和易懂性。
```

### 10.3 配套动态任务提示词（可拼接调用）

动态任务提示词由 AI Agent 调度服务根据用户当前操作场景自动选择，并拼接在常驻系统提示词、用户档案、会话上下文之后。

#### 10.3.1 每日学习计划生成专用提示词

```text
基于用户当前英语水平、学习目标、每日可用学习时长、历史薄弱知识点、生词错题遗忘进度，为用户生成今日专属定制英语学习计划。

计划必须包含：旧知识复习、新知识学习、口语 / 写作输出练习、短板专项训练四大模块。

请标注每一项任务的预估耗时、学习价值。任务总量必须适中无过载，贴合用户能力层级，拒绝超纲内容。
```

#### 10.3.2 情景口语对话专用提示词

```text
开启自适应英语情景角色扮演，匹配用户学习目标与英语等级，选择适配真实场景。

请以自然真人对话形式和用户交流，单次仅发起单轮对话，不一次性抛出多个问题。

用户完成发言后正常承接对话。单轮对话结束后统一复盘：汇总发音问题、语法错误、中式表达、可替换地道句型，给出优化建议，并同步记录用户短板。
```

#### 10.3.3 写作批改专用提示词

```text
作为专业英语写作私教，对用户提交的英文内容进行全方位精细化批改。

请严格输出：
1. 拼写、标点、基础语法错误明细；
2. 中式英语、不当用词、语序问题优化；
3. 句式升级、高级词汇替换、逻辑衔接优化；
4. 完整优化范文；
5. 本次写作核心知识点与避坑要点。

请自动提取用户写作薄弱点，并纳入用户学习档案。
```

#### 10.3.4 生词复习专项提示词

```text
调取用户个人生词库与错题词汇，基于艾宾浩斯遗忘规则筛选今日需复习词汇。

摒弃机械罗列背诵模式，采用「英文释义 + 场景例句 + 正误对比 + 随机小测试」模式帮助用户巩固。

请随机抽查记忆效果，标记未掌握词汇，并持续迭代复习计划。
```

#### 10.3.5 知识点问答讲解专用提示词

```text
针对用户提问的单词、语法、句式、翻译、场景表达等问题，结合用户英语水平通俗讲解。

请少用专业晦涩术语，多用实战例句。讲解完成后配套 1-2 个简单练习题帮助用户巩固，同时关联用户历史同类错误，针对性提醒避坑。
```

### 10.4 Agent 提示词加载架构规则

| 组成 | 加载规则 | 存储位置 | 生命周期 |
|---|---|---|---|
| 常驻系统提示词 | AI 调度服务启动时加载，全局生效，不可被普通任务覆盖 | 本地文件 + 数据库版本记录 | 长期 |
| 用户档案数据 | 每次请求实时从 MySQL 读取并动态拼接 | MySQL `user_learning_profile` 等表 | 长期，持续更新 |
| 动态任务提示词 | 根据用户当前操作场景自动匹配拼接，场景切换即时替换 | 本地文件 + `prompt_version` 表 | 中长期，可版本化 |
| 会话上下文 | Redis 缓存短期对话记录，保证对话连贯，自动清理过期上下文 | Redis | 短期，TTL 控制 |
| 用户本轮输入 | 作为最低优先级任务输入，不能覆盖系统规则 | 请求体 | 单次请求 |

#### 10.4.1 Prompt 拼接伪流程

```text
load_system_prompt()
  ↓
load_user_learning_profile(user_id)
  ↓
load_session_context(user_id, session_id, scene)
  ↓
select_task_prompt(scene)
  ↓
compose_messages(system_prompt, profile, context, task_prompt, user_input)
  ↓
call_llm(messages)
  ↓
parse_structured_result()
  ↓
update_user_memory_and_context()
```

#### 10.4.2 关键工程约束

- 常驻系统提示词必须具备最高优先级，不允许被用户输入或动态任务提示词覆盖。
- 用户档案必须由服务端可信数据源拼接，禁止客户端直接提交完整用户画像覆盖服务端记忆。
- 动态任务提示词必须按场景白名单选择，禁止客户端任意指定 Prompt 文件路径。
- Redis 会话上下文必须设置 TTL，避免长期膨胀与上下文污染。
- AI 返回内容必须经过结构化解析与 Schema 校验后再写入数据库。
- 写入用户薄弱点、生词、错题前必须保留来源场景与时间，便于后续复盘与纠错。

---

## 11. 附录：架构落地检查清单

### 11.1 工程初始化检查

- [ ] Flutter App 工程已初始化。
- [ ] Go Gin 业务服务已初始化。
- [ ] Python FastAPI AI Agent 服务已初始化。
- [ ] MySQL / Redis / MinIO 已纳入 Docker Compose。
- [ ] Swagger 文档可访问。

### 11.2 AI Agent 检查

- [ ] 支持用户长期学习档案读取。
- [ ] 支持系统提示词与任务提示词拼接。
- [ ] 支持 Redis 会话上下文。
- [ ] 支持 AI 返回结果结构化解析。
- [ ] 支持错误、生词、薄弱点自动沉淀。

### 11.3 数据库检查

- [ ] 核心表已完成建模。
- [ ] 用户 ID、任务日期、会话 ID、复习时间等高频字段已建索引。
- [ ] 学习档案、错题、生词支持 AI 个性化查询。
- [ ] 音频文件仅存对象存储 URL，不直接写入数据库。

### 11.4 部署检查

- [ ] 本地 Docker Compose 可一键启动。
- [ ] 开发、测试、生产环境变量隔离。
- [ ] CI/CD 至少包含构建、测试、镜像打包、健康检查。
- [ ] 日志按服务统一输出。

---

## 结束语

本文档定义 EasiTalk AI 个性化英语学习 App V1.0 的系统架构设计边界与工程落地规范。后续代码实现、接口设计、数据库建模、AI Agent 开发与部署流程均应围绕本文档进行拆解执行，并在每次架构调整后同步更新本文档版本。

